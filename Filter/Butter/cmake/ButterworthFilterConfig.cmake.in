@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/ButterworthFilterTargets.cmake")

# Define variables to be used by the user
set(ButterworthFilter_INCLUDE_DIRS "${CMAKE_CURRENT_LIST_DIR}/../")
set(ButterworthFilter_LIBRARIES ButterworthFilter)

# Provide information to the user
set(ButterworthFilter_FOUND TRUE)
